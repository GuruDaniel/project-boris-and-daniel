#pragma once
#include <iostream>
using namespace std;
class EHelper
{
public:
	EHelper();
	~EHelper();
	string decrypt(string message); //decrypting the whole message, keeping the code at the beggining.
	//TODO: add a message class that will contained he parts seperately.

private:

};
